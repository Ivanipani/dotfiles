jj_config_home := "$HOME/.config/jj"
jjui_config_home := "$HOME/.config/jjui"
pwd := absolute_path(".")

# Display this help
default:
    @just --list

# Create symlinks
setup:
    mkdir -p {{jj_config_home}}
    mkdir -p {{jjui_config_home}}
    ln -sf {{pwd}}/jj/config.toml {{jj_config_home}}/config.toml
    ln -sf {{pwd}}/jj/jjui-config.toml {{jjui_config_home}}/config.toml
    ln -sf {{pwd}}/git/gitconfig $HOME/.gitconfig
    ln -sf {{pwd}}/git/gitignore_global $HOME/.gitignore_global

# Install jj, diff tools
[linux]
install:
    cargo install --locked difftastic
    cargo install --locked --bin jj jj-cli
    cargo install lazyjj --locked
    sudo apt install lazygit

# Install jj, diff tools
[macos]
install:
    brew install difftastic
    brew install jj
    cargo install lazyjj --locked
    brew install lazygit

